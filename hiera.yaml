---
version: 5
hierarchy:
  - name: "distro release"
    backend: yaml
    path: "distro/%{::facts.lsbdistid}/%{::facts.lsbdistrelease}"
  - name: "distro"
    backend: yaml
    path: "distro/%{::facts.lsbdistid}"
  - name: "common"
    backend: yaml
    path: "common"
