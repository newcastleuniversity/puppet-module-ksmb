---
version: 5
hierarchy:
  - name: distro release
    path: "distro/%{::facts.lsbdistid}/%{::facts.lsbdistrelease}"
  - name: distro
    path: "distro/%{::facts.lsbdistid}"
  - name: common
    path: common
defaults:
  data_hash: yaml_data
  datadir: data
