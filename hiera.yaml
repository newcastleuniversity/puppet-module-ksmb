---
version: 4
datadir: data
hierarchy:
  - name:    'distro release'
    path:    "distro/%{::facts.lsbdistid}/%{::facts.lsbdistrelease}"
    backend: yaml
  - name:    'distro'
    path:    "distro/%{::facts.lsbdistid}"
    backend: yaml
  - name:    'common'
    path:    'common'
    backend: yaml
